
MAINTAINERCLEANFILES = Makefile.in

STYLESHEET_FILES = $(srcdir)/*xsl
EXTRA_DIST = $(STYLESHEET_FILES)
STYLESHEET_INSTALL_DIR = $(prefix)/ferris/stylesheets

stylesheetdir = $(STYLESHEET_INSTALL_DIR)
stylesheet_DATA = $(STYLESHEET_FILES)


