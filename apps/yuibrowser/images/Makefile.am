# A list of all the files in the current directory which can be regenerated
MAINTAINERCLEANFILES = Makefile.in

DATAS = $(srcdir)/*svg
webdir = /var/www/html/ferris/images
web_DATA = $(DATAS)
EXTRA_DIST = $(DATAS)

