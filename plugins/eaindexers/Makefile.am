
EXTRA_DIST = 

if HAVE_LDAP
LDAPDIR = ldap
endif

if HAVE_GCJLUCENE
GCJLUCENEDIR = lucene
endif

if HAVE_DTL
DTLDIR = odbc
endif

if HAVE_LIBPQXX
LIBPQXXDIR = postgresql
endif

if HAVE_SOPRANO
SOPRANODIR = soprano
endif

if HAVE_XAPIAN
XAPIANDIR = xapian
endif

if HAVE_CLUCENE
CLUCENEDIR = clucene
endif

if HAVE_QTSQL
QTSQLDIR = qtsql
endif

SUBDIRS = $(LDAPDIR) $(GCJLUCENEDIR) $(DTLDIR) $(LIBPQXXDIR) $(SOPRANODIR) $(XAPIANDIR) $(CLUCENEDIR) \
	null federation boost $(QTSQLDIR) 


